{
  "name": "civic-lens",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix civic-lens-frontend && npm install --prefix backend",
    "dev": "concurrently -n FRONTEND,BACKEND -c cyan,yellow \"npm run dev --prefix civic-lens-frontend\" \"npm run dev --prefix backend\"",
    "dev:frontend": "npm run dev --prefix civic-lens-frontend",
    "dev:backend": "npm run dev --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
